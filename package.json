{
  "scripts": {
    "build": "bun build --compile --target=browser ./index.html --outdir=dist && cp og-image.png googlee4be4edff7cf1d5f.html dist/",
    "deploy": "bun run build && npx wrangler pages deploy ./dist --project-name=speedup-video --branch=main"
  },
  "dependencies": {
    "mediabunny": "^1.35.0"
  }
}